<table><tr><th colspan="{{ count($headings) }}" style="font-size:18px">{{ $title }}</th></tr><tr><td colspan="{{ count($headings) }}">{{ $systemSettings['facility_name'] ?? 'Rural Health Unit' }} · {{ $summary }} · {{ now()->format('Y-m-d H:i') }}</td></tr><tr>@foreach($headings as $heading)<th style="background:#d9f3ee;border:1px solid #777">{{ $heading }}</th>@endforeach</tr>@foreach($rows as $row)<tr>@foreach($row as $cell)<td style="border:1px solid #aaa">{{ $cell }}</td>@endforeach</tr>@endforeach</table>
