@extends('layouts.app')
@section('title',$category->exists?'Edit Category':'Add Category')
@section('content')
<div class="page-header"><div><div class="breadcrumb"><a href="{{ route('categories.index') }}">Categories</a><x-icon name="chevron" class="icon-sm" /> {{ $category->exists?'Edit':'Add' }}</div><h1 class="page-title">{{ $category->exists?'Edit category':'Add category' }}</h1><p class="page-subtitle">Use a short, stable code for imports and reporting.</p></div></div>
<form class="card" method="POST" action="{{ $category->exists?route('categories.update',$category):route('categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif<div class="card-header"><h2 class="card-title">Category details</h2></div><div class="card-body"><div class="form-grid"><div class="field"><label class="required" for="code">Code</label><input class="input mono" id="code" name="code" value="{{ old('code',$category->code) }}" required placeholder="ANTI"></div><div class="field"><label class="required" for="name">Category name</label><input class="input" id="name" name="name" value="{{ old('name',$category->name) }}" required></div><div class="field span-2"><label for="description">Description</label><textarea class="textarea" id="description" name="description">{{ old('description',$category->description) }}</textarea></div><div class="field"><label class="required" for="is_active">Status</label><select class="select" id="is_active" name="is_active"><option value="1" @selected(old('is_active',$category->is_active)!==false)>Active</option><option value="0" @selected((string)old('is_active',$category->is_active)==='0')>Inactive</option></select></div></div><div class="form-actions"><a class="btn btn-secondary" href="{{ route('categories.index') }}">Cancel</a><button class="btn btn-primary" type="submit">{{ $category->exists?'Save changes':'Create category' }}</button></div></div></form>
@endsection
